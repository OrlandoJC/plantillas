<%-include("partials/head"); %>
    <div class="container">
        <h1 class="text-light">Productos ✨</h1>
        <% if (products.length> 0) { %>
            <table class="table table-light table-striped align-top">
                <thead>
                    <tr>
                        <th scope="col">Nombre</th>
                        <th scope="col">Precio</th>
                        <th scope="col">Foto</th>
                    </tr>
                </thead>
                <tbody>
                    <% products.forEach(function(product) { %>
                        <tr>
                            <td><%= product.name %></td>
                            <td><%= product.price %></td>
                            <td><img src="<%= product.pictureUrl %>" alt="" width="40"></td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
            <%} else { %>
                <div class="alert alert-danger" role="alert">
                    No hay productos 😥!
                </div>
            <% } %>
                <button type="button" class="btn  btn-outline-primary "> <i class="fas fa-arrow-left"></i> <a
                class="text-white text-decoration-none" href="/">Añadir producto</a></button>
    </div>

    <%-include("partials/footer"); %>